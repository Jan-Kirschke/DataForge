'''
evaluation.py:

Implementiere calculate_regression_metrics(y_true, y_pred) und calculate_classification_metrics(y_true, y_pred)
Erstelle plot_confusion_matrix(y_true, y_pred)
Entwickle plot_roc_curve(y_true, y_pred_proba)
Schreibe perform_cross_val_evaluation(model, data, target, cv=5)
Implementiere plot_residuals(y_true, y_pred) für Regressionsmodelle
Füge feature_importance_analysis(model, feature_names) hinzu
Erstelle eine Hauptfunktion evaluate_model(model, X_test, y_test) für umfassende Evaluation
'''